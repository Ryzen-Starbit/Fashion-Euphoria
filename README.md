# Malang — Static Landing

This is a small static landing project (HTML/CSS/JS).

How to run locally:

1. Open `index.html` in a browser.

Notes:
- Files: `index.html`, `styles.css`, `script.js`, `s1.avif`.
- To publish, create a remote repository and push the project.
